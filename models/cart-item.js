class CartItem {
    constructor(id, product, size, options, price ) {
      this.key = id,
      this.product = product;
      this.size = size;
      this.options = options;
      this.price = price;
    }
  }
  
  export default CartItem;